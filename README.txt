if the minilibx-linix does not have files get them from (here)[URL=https://github.com/42Paris/minilibx-linux.git]

to compile run "make". for linux do "make linux"

then to run do "./ant <num of ants>"
